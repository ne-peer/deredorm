export class Idol {

    constructor(
        // 識別子
        public id: number,
        // 名前
        public name: string,
        // 属性
        public type: string,
        // 別名
        public model: string,
    ) { }

}